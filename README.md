# Parasite Black game repo

Clone into an empty folder, then open that folder as a Renpy project

File format rundown: .png/.webp for images, .webm for videos/anims, .ogg for audio

.gitignore is set up to ignore everything except what is necessary, so

Convert or die
